[tool.poetry]
name = "Ancalagon"
version = "0.0.2a1"
description = "The blackest ASGI framework"
authors = ["Nimond <dndnomin@gmail.com>"]
license = "gpl-3.0"

[tool.poetry.dependencies]
python = "^3.6"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
